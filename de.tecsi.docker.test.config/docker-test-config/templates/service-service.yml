kind: Service
apiVersion: v1
metadata:
  name: test-service
  labels:
    run: test-service
spec:
#  type: LoadBalancer
  type: NodePort
  ports:
  - name: service-port
    port: 8080
    nodePort: 32308
    targetPort: 8080
    protocol: TCP
  - name: debug-port
    port: 50506
    targetPort: 50506
    protocol: TCP
  selector:
    app: test-service

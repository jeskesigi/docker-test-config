kind: Service
apiVersion: v1
metadata:
  name: test-internal
  labels:
    run: test-internal
spec:
  type: ClusterIP
  ports:
  - name: internal-port
    port: 8081
    protocol: TCP
  - name: debug-port
    port: 50505
    protocol: TCP
  selector:
    app: test-internal

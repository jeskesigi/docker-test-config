kind: Service
apiVersion: v1
metadata:
  name: test-internal
  labels:
    run: test-internal
spec:
  type: ClusterIP
  ports:
  - name: internal-port
    port: {{ .Values.test_internal_port }}
    protocol: TCP
  selector:
    app: test-internal
